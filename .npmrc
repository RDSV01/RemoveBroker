# Pas de statistiques d'installation, pas de bruit dans la sortie.
fund=false
audit=false

# Le serveur utilise playwright-core: aucun navigateur n'est telecharge a
# l'installation. Ils le sont a la demande depuis l'interface, ou avec:
#   npm run browsers:install
